// PDF Service placeholder
// Este servicio se puede implementar más adelante si es necesario

const pdfService = {
  generateReport: async (data: any) => {
    console.log('PDF Service: generateReport called with', data);
    // Implementación futura
    return { success: true, message: 'PDF service not implemented yet' };
  }
};

export default pdfService;
